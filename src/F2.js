import React, { useContext } from 'react';
import { FormContext } from './F1';
import F3 from './F3'

const F2 = () => {
  //const { formData, setFormData } = useContext(FormContext);

  // Handle input changes and update the context state
  
  return (
    <F3/>
  );
};

export default F2;